<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="gallery">


<insert id="insertImage"  parameterType="com.javaex.vo.AttachVo">
<![CDATA[

	insert into gallery (orgName, saveName, filePath, fileSize)
	 values(#{orgName}, #{saveName}, #{filePath}, #{fileSize})
	
]]>
</insert>


<insert id="insert"  parameterType="com.javaex.vo.GalleryVo">
<![CDATA[

	insert into gallery (orgName, saveName, filePath, fileSize)
	 values(#{orgName}, #{saveName}, #{filePath}, #{fileSize})
	
]]>
</insert>

<select id="selectList" parameterType="com.javaex.vo.AttachVo" resultType="com.javaex.vo.AttachVo">>

</select>














</mapper>
	